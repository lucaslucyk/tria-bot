{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "http",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/main.go",
            // "args": ["-env-filename=${workspaceFolder}/.env"],
            "envFile": "${workspaceFolder}/.env"
            // go run ${PWD}/cmd/main.go -env-filename=${ENV_FILE}
        }
    ]
}